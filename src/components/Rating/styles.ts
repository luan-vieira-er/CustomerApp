import styled from 'styled-components';

export const StyledText = styled.p`
    color: ${({ theme }) => theme.text.gray350};
    font-weight: 400;
    font-size: 14px;
    line-height: 18px;
`;
